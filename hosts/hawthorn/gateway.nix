# Configuration du gateway (Caddy + Cloudflare Tunnel)
# Ce fichier sera rempli dans la Phase P3 du plan de migration

{ config, lib, pkgs, ... }:

{
  # Configuration Caddy (à implémenter en T11-T15)
  # services.caddy = { ... };

  # Configuration Cloudflare Tunnel (à implémenter en T24-T26)
  # systemd.services.cloudflared = { ... };
}
