# Memos — variables applicatives
# Les secrets sont injectés depuis secrets/poppy.yaml (SOPS)
# Ce fichier ne contient que des valeurs non-sensibles

MEMOS_MODE=prod

# Backup destination (nom du dossier distant rclone)
MEMOS_BACKUP_REMOTE=memos