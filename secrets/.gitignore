# Ne jamais committer les fichiers de secrets non chiffrés
*.yaml
*.yml
*.json

# Autoriser les fichiers d'exemple
!*.example
!.gitignore

# Les fichiers chiffrés par sops sont autorisés (ils contiennent sops: dans le contenu)
# Mais par sécurité, on préfère les committer explicitement avec git add -f
