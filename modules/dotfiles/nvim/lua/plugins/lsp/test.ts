interface User {
  name: string;
  age: number;
}

const user: User = {
  name: "John",
  age: 30
};

console.log(user); // Place ton curseur après le point
user.age = TextDecoderStrea

// Place ton curseur après le point 
//
// Le serveur LSP devrait suggérer des propriétés de l'interface User 
// // comme 'name' et 'age' lorsque tu tapes '.' après 'user.'
// m.prototype; // Place ton curseur ici
useruser.
