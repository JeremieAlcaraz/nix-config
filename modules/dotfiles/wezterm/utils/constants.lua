-- ══════════════════════════════════════════════════════════════════════
-- ~/.config/wezterm/utils/constants.lua - Constantes partagées
-- ══════════════════════════════════════════════════════════════════════

local M = {}

-- ═══════════════════════════════════════════════════════════════════
-- Chemins système
-- ═══════════════════════════════════════════════════════════════════
M.PATHS = {
  HOME = os.getenv("HOME"),
  NVIM_DATA = os.getenv("HOME") .. "/.local/share/nvim",
  HOMEBREW_BIN = "/opt/homebrew/bin",
}

-- ═══════════════════════════════════════════════════════════════════
-- Extensions de fichiers pour la recherche
-- ═══════════════════════════════════════════════════════════════════
M.FILE_EXTENSIONS = {
  CODE = { "lua", "py", "js", "ts", "jsx", "tsx", "rs", "go" },
  CONFIG = { "yaml", "yml", "json", "toml", "conf" },
  TEXT = { "md", "txt", "rst" },
}

-- Toutes les extensions combinées
M.ALL_EXTENSIONS = {}
for _, group in pairs(M.FILE_EXTENSIONS) do
  for _, ext in ipairs(group) do
    table.insert(M.ALL_EXTENSIONS, ext)
  end
end

-- ═══════════════════════════════════════════════════════════════════
-- Messages et emojis
-- ═══════════════════════════════════════════════════════════════════
M.MESSAGES = {
  SEARCH_START = "🔍 Recherche des fichiers récents...",
  SUCCESS = "✅ Fichiers trouvés via: %s",
  NO_FILES = "❌ Aucun fichier trouvé",
  OPENING = "🚀 Ouverture: %s",
}

M.EMOJIS = {
  DATABASE = "📊",
  TELESCOPE = "🔭", 
  MEMORY = "💾",
  LIGHTNING = "⚡",
  FOLDER = "📁",
  SEARCH = "🔎",
  HOME = "🏠",
}

return M
