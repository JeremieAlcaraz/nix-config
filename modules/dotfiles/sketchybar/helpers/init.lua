-- Add the sketchybar module to the package cpath
package.cpath = package.cpath .. ";/Users/" .. os.getenv("USER") .. "/.local/share/sketchybar_lua/?.so"

HELPERS_DIR = os.getenv("HOME") .. "/.local/share/sketchybar/helpers"
